CREATE TABLE productos
(
    id    int not null primary key auto_increment,
    nombre  VARCHAR(50)    NOT NULL,
    tipo VARCHAR(50)    NOT NULL,
    precio int NOT NULL,
    fecha_actualizada VARCHAR(50) NOT NULL,

);